# History: 2026-04-04T00:10

## Shelf System Testing Discussion
- First shelf completed successfully — wrote chunk to `.claude/history/2026-04-04T00-00-00.md` and TOC to `.claude/history/toc.md` [tested]
- User asked which `.claude` directory was being written to — confirmed it's the project's `.claude/history/` under the project root, matching PLAN.md spec [user, corrected: user wanted to verify correct directory]

## Shelf Retrieval Testing — Key Insight
- User asked how to trigger a TOC lookup and verify it works [user]
- Suggested asking specific compression questions like "What's the difference between CABAC and CAVLC?" [inferred]
- User correctly identified the fundamental problem: there's no way to distinguish "read from shelf" vs "answered from training knowledge" for general knowledge topics [user]
- **Conclusion**: Testing shelf retrieval requires conversation-specific content that the LLM couldn't know from training — project decisions, unique details, specific file paths discussed [inferred]
- Real end-to-end test requires: (1) shelf, (2) compaction or new session so original context is gone, (3) ask about shelved detail, (4) verify Read tool call on chunk file [inferred]
- Current session hasn't hit compaction yet, so everything is still in context window — shelf wrote to disk correctly but retrieval can't be truly tested yet [inferred]

## Unresolved
- Need to actually test shelf retrieval after context is gone (new session or post-compaction)
